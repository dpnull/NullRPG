using NullRPG.GameObjects;
using System;
using System.Collections.Generic;
using System.Text;

namespace NullRPG.ItemTypes
{
    class GatherableItem : Item
    {
        public GatherableItem(int id, string name, int gold, int level, int defense, int health, bool isUnique)
             : base(id, name, gold, isUnique, level, 0, 0, defense, health)
        {

            public static GatherableItem 
        }
    }
}
